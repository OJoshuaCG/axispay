<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Public API v1
|--------------------------------------------------------------------------
|
| Registered in bootstrap/app.php on the API host with the `/v1` prefix and
| the `api` middleware group. Endpoints arrive in Phase 3 (plan section 27);
| keep docs/api/openapi.yaml in sync with every route added here.
|
*/
